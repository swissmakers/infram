{
  "name": "infram-connector",
  "version": "1.2.0",
  "license": "SEE LICENSE IN ../LICENSE",
  "private": true,
  "scripts": {
    "dev": "cargo tauri dev",
    "build": "cargo tauri build",
    "client:dev": "cd ../client && yarn dev",
    "client:build": "cd ../client && yarn build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
